{
  "name": "drushti-portfolio",
  "private": true,
  "scripts": {
    "dev": "npm install && npm install --prefix client && npm install --prefix server && docker compose up -d postgres && concurrently -n \"client,server\" -c \"cyan,green\" \"npm run dev --prefix client\" \"npm run dev --prefix server\"",
    "docker:up": "docker compose up -d --build",
    "docker:down": "docker compose down"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
